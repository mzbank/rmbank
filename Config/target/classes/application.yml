

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mzbank/config
          username: mzbank
          password: at2016year
          search-paths: config-client

server:
  port: 8090
